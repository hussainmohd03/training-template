[project]
name = "fine-tune-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bitsandbytes>=0.50.2",
    "datasets>=5.0.1",
    "peft>=0.21.0",
    "transformers[torch]>=5.17.0",
    "trl>=1.13.0",
]
